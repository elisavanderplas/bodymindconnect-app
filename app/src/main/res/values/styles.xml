<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base text style that applies to ALL text -->
    <style name="AppTextStyle" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Apply to buttons -->
    <style name="Widget.App.Button" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>